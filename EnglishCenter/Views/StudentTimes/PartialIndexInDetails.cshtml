@model IEnumerable<EnglishCenter.Models.StudentTime>

@{
    //ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}
<table class="table">
    <tr>
        @*<th>
            @Html.DisplayNameFor(model => model.Person.Name)
        </th>*@
        <th>
            <span></span>
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Future)
        </th>
    </tr>

    @foreach (var item in Model)
    {
        <tr>
            @*<td>
                @Html.DisplayFor(modelItem => item.Person.Name)
            </td>*@
            <td>
                @Html.DisplayFor(modelItem => item.Studied)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Future)
            </td>
        </tr>
    }

</table>

