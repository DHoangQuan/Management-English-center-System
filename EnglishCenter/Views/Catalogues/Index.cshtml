@model IEnumerable<EnglishCenter.Models.Catalogue>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.CatalogueName)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.CatalogueName)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.Catalogue1 }) |
            @Html.ActionLink("Details", "Details", new { id=item.Catalogue1 }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.Catalogue1 })
        </td>
    </tr>
}

</table>
