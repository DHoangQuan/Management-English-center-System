@model IEnumerable<EnglishCenter.Models.QuantityAndAttendance>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Person.Name)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.UsingRoom.RoomID)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Attendent)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Person.Name)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.UsingRoom.RoomID)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Attendent)
        </td>
        <td>
            @*@Html.ActionLink("Edit", "Edit", new { id=item.QnAID }) |
            @Html.ActionLink("Details", "Details", new { id=item.QnAID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.QnAID })*@
            @Html.ActionLink("IndexPerSonal", "Authentications", new { id = item.PeopleID })
        </td>
    </tr>
}

</table>
